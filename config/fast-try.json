{
    "version": "2.0",
    "columns": 4,
    "tiles": [
        {
            "type": "PORT",
            "label": "fast-try frontend",
            "params": {
                "hostname": "fast-try.illacloud.com",
            "port": 443

            }
        },
        {
            "type": "PORT",
            "label": "fast-try api",
            "params": {
                "hostname": "fast-try-api.illacloud.com",
            "port": 443

            }
        },
        {
            "type": "PORT",
            "label": "fast-try websocket",
            "params": {
                "hostname": "fast-try-ws.illacloud.com",
                "port": 443
            }
        },
        {
            "type": "HTTP-RAW",
            "label": "fast-try release version",
            "params": {
                "url": "https://fast-try.illacloud.com/user/login",
                "regex": "release-version=\"([a-zA-Z0-9].*?)\""
            }
        },
        {
            "type": "PORT",
            "label": "fast-try frontend test",
            "params": {
                "hostname": "fast-try-test.illacloud.com",
                "port": 443
            }
        },
        {
            "type": "PORT",
            "label": "fast-try api test",
            "params": {
                "hostname": "fast-try-api-test.illacloud.com",
                "port": 443
            }
        },
        {
            "type": "PORT",
            "label": "fast-try websocket test",
            "params": {
                "hostname": "fast-try-ws-test.illacloud.com",
                "port": 443
            }
        },
        {
            "type": "HTTP-RAW",
            "label": "fast-try release version test",
            "params": {
                "url": "https://fast-try-test.illacloud.com/user/login",
                "regex": "release-version=\"([a-zA-Z0-9].*?)\""
            }
        }
    ]
}
